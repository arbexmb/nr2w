# frozen_string_literal: true

RSpec.describe Nr2w do
  it "has a version number" do
    expect(Nr2w::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
